name: notification_cleaner
description: Updated fork of clear_all_notifications
version: 0.0.1
homepage: https://github.com/mr-gami001/notification_cleaner

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.notification_cleaner
        pluginClass: NotificationCleanerPlugin
      ios:
        pluginClass: NotificationCleanerPlugin

